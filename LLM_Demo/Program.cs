#region Init
Console.OutputEncoding = System.Text.Encoding.UTF8;
Console.InputEncoding = System.Text.Encoding.UTF8;
#endregion

//await new 异常单基本查询不准确回答().执行();

//await new 异常单基本查询().执行();

//await new 底层逻辑().执行();

//await new 异常单基本查询加定义().执行();

//await new 自动生成SQL查询().执行();

//await new 自动生成汇总SQL查询().执行();

//await new 不支持FunctionCalling().执行();

//await new KM批量生成历史数据embeddings保存VectorStore().执行();

//await new KM批量生成历史数据embeddings保存VectorStore_验证().执行();

await new 排程器_定期更新异常解决方式参考().执行();









